import useLogger from '@voerka/logger'
import pkg from '../package.json'

// Registrar la impresora de registrosconst { name, version } = pkg
export default useLogger('mlogger', { prefixs: [`${name}@${version}`] })
